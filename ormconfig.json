{
	"type": "postgres",
  "host": "localhost",
  "port": 5432,
  "database": "plantmanager",
  "username": "postgres",
  "password": "plantmanager",
  "migrations": [
    "./src/database/migrations/*.ts"
  ],
  "entities": [
    "./src/entities/*.ts"
  ],
  "cli": {
    "migrationsDir": "./src/database/migrations"
  }
}
